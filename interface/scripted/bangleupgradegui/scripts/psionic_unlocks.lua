require "/scripts/util.lua"
require "/scripts/interp.lua"

function init()
  self.elements = {"physical"}
  self.shotTypes = {"basic"}
end

function addElement(element)

end

function addShotType(shotType)

end
